package curtidor.utils;

import lejos.robotics.navigation.WayPoint;
import lejos.robotics.navigation.WayPointListener;

/**
 * Interface for informing listeners that a way point has been reached.
 * 
 */
public interface WayPointListenerInicio extends WayPointListener
{
	public void wayPointComplete(WayPoint wp);
	
}

